# Positive fixture for the vagrant box pin rule. See test docstring.
# The box line below carries no accompanying pin in this file.
Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-22.04"
  config.vm.hostname = "victim"
end
